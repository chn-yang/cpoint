#include "main.h"

int find_int(int key, int array[], int length);
int num_length(long num);
int is_rev_num(long num);
void num_explode(long num, int arr[], int len);
int point_fun(int num);
// void binary_to_ascii(unsigned int value);